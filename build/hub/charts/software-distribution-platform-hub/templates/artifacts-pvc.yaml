apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.basic.component }}-artifacts
  namespace: {{ .Values.namespace }}
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ .Values.artifact.storage }}
